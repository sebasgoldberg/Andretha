{% extends '::base.html.twig' %}

{%block content%}

<h1>CarpetaEmprendimiento creation</h1>

<form action="{{ path('carpetaemprendimiento_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('carpetaemprendimiento') }}">
            Back to the list
        </a>
    </li>
</ul>

{%endblock%}

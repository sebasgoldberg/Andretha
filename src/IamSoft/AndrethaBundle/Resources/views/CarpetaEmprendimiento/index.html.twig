{% extends 'IamSoftAndrethaBundle:Default:base.html.twig' %}

{%block content%}

<h1>CarpetaEmprendimiento list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Emprendimiento</th>
            <th>Carpeta</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('carpetaemprendimiento_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.carpeta }}</td>
            <td>{{ entity.emprendimiento }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('carpetaemprendimiento_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('carpetaemprendimiento_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('carpetaemprendimiento_new') }}">
            Create a new entry
        </a>
    </li>
</ul>

{%endblock%}

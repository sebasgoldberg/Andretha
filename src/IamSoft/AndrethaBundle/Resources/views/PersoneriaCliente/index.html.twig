{% extends 'IamSoftAndrethaBundle:Default:base.html.twig' %}

{%block content%}

<h1>PersoneriaCliente list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Cliente</th>
            <th>Escribano</th>
            <th>Personeria</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('personeriacliente_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.cliente }}</td>
            <td>{{ entity.escribano }}</td>
            <td>{{ entity.personeria }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('personeriacliente_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('personeriacliente_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('personeriacliente_new') }}">
            Create a new entry
        </a>
    </li>
</ul>

{%endblock%}

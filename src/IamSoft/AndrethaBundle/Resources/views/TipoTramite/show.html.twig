{% extends 'IamSoftAndrethaBundle:Default:base.html.twig' %}

{%block content%}

<h1>TipoTramite</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Denominacion</th>
            <td>{{ entity.denominacion }}</td>
        </tr>
        <tr>
            <th>Tipo de carpeta</th>
            <td>{{ entity.tipoCarpeta }}</td>
        </tr>
        <tr>
            <th>Tipo de tramite padre</th>
            <td>{{ entity.tipoTramitePadre }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('tipotramite') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('tipotramite_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('tipotramite_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>

{%endblock%}

{% extends 'IamSoftAndrethaBundle:Default:base.html.twig' %}

{%block content%}

<h1>LoteAnexos</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Numpreimpresoini</th>
            <td>{{ entity.numPreImpresoIni }}</td>
        </tr>
        <tr>
            <th>Cantidad</th>
            <td>{{ entity.cantidad }}</td>
        </tr>
        <tr>
            <th>Fechacompra</th>
            <td>{{ entity.fechaCompra|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Cantidadusados</th>
            <td>{{ entity.cantidadUsados }}</td>
        </tr>
        <tr>
            <th>Registro escribano</th>
            <td>{{ entity.registroEscribano }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('loteanexos') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('loteanexos_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('loteanexos_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>

{%endblock%}

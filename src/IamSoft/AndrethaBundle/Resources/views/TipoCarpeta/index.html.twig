{% extends 'IamSoftAndrethaBundle:Default:base.html.twig' %}

{%block content%}

<h1>TipoCarpeta list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Letra</th>
            <th>Denominacion</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('tipocarpeta_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.letra }}</td>
            <td>{{ entity.denominacion }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('tipocarpeta_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tipocarpeta_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('tipocarpeta_new') }}">
            Create a new entry
        </a>
    </li>
</ul>

{%endblock%}

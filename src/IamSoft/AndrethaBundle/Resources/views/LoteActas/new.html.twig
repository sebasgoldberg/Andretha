{% extends '::base.html.twig' %}

{%block content%}

<h1>LoteActas creation</h1>

<form action="{{ path('loteactas_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('loteactas') }}">
            Back to the list
        </a>
    </li>
</ul>

{%endblock%}
